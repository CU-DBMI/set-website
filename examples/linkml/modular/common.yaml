id: https://example.org/coffee-common
name: coffee_common
prefixes:
  ex: https://example.org/
  linkml: https://w3id.org/linkml/
imports:
  - linkml:types
default_prefix: ex
default_range: string

enums:
  OrderStatusEnum:
    permissible_values:
      queued:
      brewing:
      ready:
